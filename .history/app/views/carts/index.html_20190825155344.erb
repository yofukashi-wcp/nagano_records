    <h3>ショッピングカート</h3>

<!-- <%= form_with url: new_order, local: true do |f| %> -->
<% @carts.each do |cart| %>
<table>

    <th></th>
    <th></th>
    <th></th>
    <tr>
        <td text-align: center;>
            <img height= "80" src="<%= attachment_image_tag cart, : %>"/>
        </td>
        <td>
            <p>商品名:<%= cart.product.name %></p>
        </td>
    </tr>
</table>